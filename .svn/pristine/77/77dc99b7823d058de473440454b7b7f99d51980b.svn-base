// CTA modules
////////////////////////
%CTA-module {
  @include margin(null $small-spacing);
  background-color: $clr-red;
  position: relative;
  text-align: center;

  @include media($mobile-up) {
    @include margin(null auto);
    max-width: 22em;
  }

  @include media($tablet-up) {
    height: 6.5em;
    margin-bottom: $small-spacing;
    //max-width: 48em;
    max-width: 46em;
    text-align: left;
  }

  figure {
    @include media($tablet-up) {
      @include margin(0 $base-spacing null null);
      float: left;
      height: 100%;
    }
  }

  img {
    border-bottom-right-radius: $base-spacing * 2;
    width: 100%;

    @include media($tablet-up) {
      @include size(auto 100%);
      border-bottom-right-radius: 0;
      border-top-right-radius: $base-spacing * 2;
    }
  }

  h1 {
    font-size: 1.25em;
    margin: $small-spacing;

    @include media($tablet-up) {
      margin: 0 $small-spacing $small-spacing / 2;
      padding-top: $small-spacing;
    }

    @include media($desktop-up) {
      font-size: 1.5em;
    }
  }

  p {
    @include margin(null $small-spacing $small-spacing);
    font-size: 1em;
    line-height: 1.3;
  }
}

.CTA-module_enews {
  @extend %CTA-module;
  @extend %CTA-type;

  form#CTA-enews {
    @include margin(null $small-spacing);
    position: relative;
    top: $small-spacing;

    @include media($mobile-to-tablet) {
      @include margin(null $base-spacing)
    }

    @include media($tablet-up) {
      @include position(absolute, auto $small-spacing auto null);
      //@include position(absolute, auto $small-spacing $base-spacing * -1 null);
      width: 50%;
    }
  }

  input#CTA-email {
    margin-bottom: 0;
  }

  button#CTA-modal-trigger {
    @include padding(null $small-spacing);
    @include position(absolute, 1px 1px null null);
    background-color: transparent;
    border: 0;
    letter-spacing: .1em;
    line-height: 1.2;
    margin: 0;
    outline: none;
    text-transform: uppercase;

    &:hover {
      background-color: $clr-red;
      border: 1px solid $clr-red;
      right: 0;
      top: 0;
    }
  }
}

.CTA-module_subscribe {
  @extend %CTA-module;
  @extend %CTA-type;

  .button {
    @extend %base-button;
    border: 0;
    box-shadow: $form-box-shadow, $base-box-shadow;
    display: inline-block;
    font-size: 1em;
    margin: 0 $small-spacing;
    padding: $small-spacing $base-spacing $small-spacing - em(2px);
    position: relative;
    top: $small-spacing;

    @include media($tablet-up) {
      @include position(absolute, auto $small-spacing $base-spacing * -1 null);
    }
  }
}
